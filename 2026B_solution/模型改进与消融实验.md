# 模型改进与消融实验（第 5 章素材）

> 本章回答两个问题：**我们试过哪些改进、为什么最终不采用**；以及**冻结版本在什么口径下算"稳定"**。
> 全部数字来自本仓库代码：`problem3_robot/`（问题三）、`problem4_robot/`（问题四）、`2026B_solution/verify/experiment.py`（统一仿真环境）。
> 复现命令见 §5。

---

## 1 最终冻结配置

问题三、问题四的**算法结构正式冻结，不再调整**。所有开关保留在代码中（默认值即下表），仅作实验入口：

| 项目 | 冻结值 | 含义 |
|---|---|---|
| 问题三 | **冻结** | 7 点覆盖（原点 + 半径 $1200$m 六边形，`HEX_R=1200`）+ 最小覆盖圆/最小二乘定位 + 任务级 2-opt 清除队列 + 受限顺路清除 |
| 问题三：顺路清除阈值 | $\delta=300$ m | 插入增量 $\Delta L\le300$ m 才顺路清除 |
| 问题三：LS 试探清除门限 | `LS_CLEAR_GATE=None` | 不设门限：$\Omega$ 超限/退化时仍直接用交会点盲清除 |
| 问题三：机会性顺带观测 | `OPP_MEASURE=False` | 关闭（困难子集有效、全案例平均无改善，见 §2.3） |
| 问题三：概率图排序 / DOP 预筛 | `False` / `False` | 关闭 |
| 问题四：顺路清除阈值 | $\delta=300$ m | 同问题三 |
| 问题四：补测距离上限 | `SUPP_MAX_DIST=None` | 无上限（不因补测点远而跳过补测） |
| 问题四：邻域试探圈 | `NEIGHBOR_RINGS=(8.0, 15.0)` | 归航点失败后 $8$m、$15$m 两圈各 $6$ 点 |
| 问题四：网格 | `MESH_A=900`、`MESH_MARGIN=800` | 31 点 / 42 三角形 / 覆盖空洞 0 |
| 问题四：负信息 / PSO / 多方向复核 | `False` / `False` / `False` | 三个复杂模块全部关闭 |

冻结依据：结构层（覆盖、定位、清除顺序、证书）已通过确定性检验；策略层（阈值类）全部做过配对实验，**没有一个阈值被证明能稳定改善"全清率 + 时间尾部"**，故一律回到保守值。

### 1.1 两版关系（双版本叙事）

本文档与其余章节采用**双版本叙事**：论文同时保留"模型选型阶段"与"最终冻结版"两套口径，二者不是矛盾，而是同一条论证链的两个阶段。

| 维度 | 选型阶段（M1 系列 / 多环布局） | 冻结版（参赛实现） |
|---|---|---|
| 问题三布局 | 原点 + 环 $R=1200$m×8（9 站） | 7 点：原点 + $R=1200$m 六边形 6 点 |
| 问题三扫描 | 9 站全量扫 $1\sim20$ 频道 | 蛇形频道序 + 按交会角增益跳过冗余检测 |
| 问题三定位 | 最小二乘交会（交会角 $\ge45^\circ$，否则垂直补测 $300\sim500$m） | $\Omega$（半平面交可行域）最小覆盖圆 $\le20$m 判定可清除；否则最大交会角两示向交会 |
| 问题三清除 | 最近邻 TSP + 归航精定位 | 任务级最近邻 + 开放路径 2-opt + **受限顺路清除 $\delta=300$** |
| 问题三检验 | 1000 案例：$100\%$、平均定位清除 $381$s、总 $4\,864$s | 1000 案例：$100\%$、移动 $18\,462$m、检测 $136$ 次、总 $4\,371$s；实机 7 局 $3\,997.5\pm522.2$s |
| 问题四布局 | 三环 29 站（$R500\times8+R1200\times8+R2200\times12$） | 31 点确定性三角网格（$a=900$、外扩 $800$，覆盖空洞 $0\%$） |
| 问题四"无源"判定 | 无证书，靠复扫/螺旋搜索兜底 | **逐频道三角形证书**（按真实网格编号）+ `certificate_index_ok` |
| 问题四恢复机制 | 垂直补测 → 二分归航 → 螺旋搜索 → 恢复兜底 | 多方位补测 → 逐示向二分归航 → 归航点邻域 $(8,15)$ 试探 |
| 问题四检验 | 500 案例：$99.97\%$、平均 $837$s | 离线 $0/50/100\%$ 定向均 $100\%$；实机 11 局全清、$10\,784.0\pm701.8$s |
| 两版共同结论 | 确定性固定覆盖优于概率/滚动搜索；DOP、PSO 无益 | 同上；且阈值类改进（LS 门限、补测上限、$\delta$、邻域）经配对实验全部否决或维持原值 |

**两阶段各自回答的问题**：

- **选型阶段**（`实验报告_模型竞技.md`）：回答"**用哪一类模型**"——在统一仿真环境下比较 M0 随机、M1 确定性覆盖、M2 +DOP、M3/M3′ 概率与滚动、M4 混合、PSO 对照，结论是确定性批量覆盖胜出，概率/滚动、DOP、PSO 均无益。
- **冻结版阶段**（本章）：回答"**在这一类模型内如何把实现做对、把调度调到最优**"——修正证书索引正确性、统一 $\Omega$ 外接口径、任务级 2-opt 消除坐标碰撞、并用配对实验逐项审查阈值（$\delta$、LS 门限、补测上限、邻域试探）。

因此论文的完整论证链是：**模型选型（哪一类）→ 实现冻结（怎么用对）→ 消融复核（改什么没用）**。选型阶段的布局数字（9 站/29 站）与冻结版（7 点/31 点网格）不同，是同一策略思想在两轮迭代中的具体化，正文两套口径并存并各自标注。

---

## 2 未采用的三项改进

### 2.1 问题三：最小二乘试探清除的信任门限（`LS_CLEAR_GATE`）

**动机**：定位分两种方式——$\Omega$（可行域最小覆盖圆）半径 $\le20$ m 时"判定即可清除"（MEC 档）；否则退化到"交会角最大的两条示向交会"（LS 档）。LS 点定位精度更差，却与 MEC 点一样直接用于盲清除。自然猜想：只信任 $\Omega$ 半径足够小的点，可以少做无效清除。

**诊断（300 案例 / 19 322 次定位 / 4 167 次清除尝试）**：

| 定位来源 | 首次清除成功 | $\Omega$ 半径中位 |
|---|---|---|
| MEC（$\Omega\le20$ m） | **100 %** | 14.8 m |
| LS（$\Omega>20$ m） | 91.8 %（队列）/ 89.6 %（顺路） | 23.4 m，P90 34.4 m |
| LS 失败后归航补回 | 98.7 % / 93.8 % | — |

**配对实验**（同一批 300 案例，同随机种子；表内为"门限档 − 当前档"）：

| 方案 | 清除率 | Δ时间 | Δ移动 |
|---|---|---|---|
| 当前（LS 点直接盲清除） | 100 % | — | — |
| 门限 $\Omega\le40$ m | 100 % | +24.6 s | +78.6 m |
| 门限 $\Omega\le30$ m | 100 % | +421.5 s | +1880.8 m |
| 门限 $\Omega\le25$ m | 100 % | +892.9 s | +4031.6 m |
| 门限 $\Omega\le20$ m（仅 MEC） | 100 % | **+2646.0 s** | +12016.8 m |

**结论：不采用。** 门限越紧越差，且**单调劣化**。原因是决策的期望代价不对等：拒绝盲清除后必须改走补测/归航，每次要多付一次 $5$ s 检测与往返路程；而盲清除即使失败也只损失 $3$ s（失败清除）且由归航兜底。$10\%$ 的首清失败率远不足以补偿"多一次补测"的代价。

### 2.2 问题四：补测距离上限（`SUPP_MAX_DIST`）

**动机（来自实机长尾）**：11 局实机中 10 局聚集在 $10\,058\sim11\,169$ s，唯一长尾局 `p4_log_20260911_185634.jsonl` 为 $12\,676.6$ s / $45.10$ km。其额外成本集中在一个困难频道上：

| 操作 | 移动 | 时间 |
|---|---:|---:|
| 远距离补测 | 4 662.0 m | 938.4 s |
| 补测后归航 | 1 594.1 m | 358.8 s |
| 顺路失败归航 | 1 072.3 m | 265.5 s |
| 最终队列清除 | 9 792.3 m | 2 023.5 s |

该频道在网格扫描后只有 1 条有效示向，程序移动 $4.66$ km 补测，形成的交会角仍仅 $28.2^\circ$，无法定位，随后又执行二分归航。猜想：若"最近补测点太远就跳过补测、直接沿示向二分归航"，可压缩这条长尾。

**四臂配对实验**（每档 400 案例，同种子同场景；时间模型含换频与清除耗时）：

定向比例 50 %

| 配置 | 全清率 | 漏清例 | 平均(s) | P90 | P95 | 最大 | 长尾例数 | Δ时间 95 %CI | 补测距离 | 跳过/例 | 补回率 |
|---|---:|---:|---:|---:|---:|---:|---:|---|---:|---:|---:|
| 当前：无上限 | 100 % | 0 | 10 358 | 10 889 | 11 007 | 11 782 | 20 | — | 2 147 m | 0 | — |
| 上限 1 500 m | 100 % | 0 | 10 344 | 10 937 | 11 062 | 12 226 | 26 | [−69, 40] | 1 037 m | 0.05 | 100 % |
| 上限 2 500 m | 100 % | 0 | 10 366 | 10 899 | 11 007 | 12 226 | 20 | [−38, 54] | 1 620 m | 0.02 | 100 % |
| 上限 3 500 m | 100 % | 0 | 10 367 | 10 890 | 11 029 | 11 782 | 23 | [−11, 29] | 2 066 m | 0.01 | 100 % |

定向比例 100 %

| 配置 | 全清率 | 漏清例 | 平均(s) | P90 | P95 | 最大 | 长尾例数 | Δ时间 95 %CI | 补测距离 | 跳过/例 | 补回率 |
|---|---:|---:|---:|---:|---:|---:|---:|---|---:|---:|---:|
| 当前：无上限 | 100 % | 0 | 10 704 | 11 215 | 11 392 | 12 159 | 20 | — | 2 090 m | 0 | — |
| 上限 1 500 m | 100 % | 0 | 10 684 | 11 185 | 11 377 | 12 395 | 20 | [−78, 38] | 817 m | 0.06 | 100 % |
| 上限 2 500 m | 100 % | 0 | 10 705 | 11 181 | 11 367 | 13 136 | 19 | [−57, 59] | 1 451 m | 0.04 | 100 % |
| 上限 3 500 m | 100 % | 0 | 10 694 | 11 215 | 11 410 | 12 159 | 22 | [−51, 32] | 2 033 m | 0.01 | 100 % |

（长尾例数 = 时间超过基线 P95 的案例数。）

**结论：不采用。** 三点理由：

1. **硬约束满足，但目标未达成**。三档均保持全清率 $100\%$、漏清 $0$ 例、被跳频道的后续补回率 $100\%$；然而**平均时间的 95 % 置信区间全部跨 0**（−20 s ～ +9 s），**尾部最大时间在 3 档中的 2 档反而更大**（+444 s、+236 s、+977 s），长尾例数没有下降。
2. **触发率过低**。跳过仅 $0.01\sim0.06$ 次/例（400 例中 4～24 次），因此它对均值与 P95 几乎没有影响，只把少数案例推入更贵的路径。
3. **机制上无法区分好坏两类**。跳过补测等于直接二分归航：它能救"远距离补测本来也会失败"的情形，但同时也砍掉了"本来能靠补测拿到第二条有效示向、从而便宜清除"的情形。程序在测量前**无法预知补测能否形成可用交会角**，所以距离这一可观测信息**不足以判断补测的信息价值**。

> **关于 P99 的说明**：400 例规模下 P99 由约 4 个样本决定，稳定性有限，故本章**不强调任何单档的 P99 改善**。可靠结论是："全清率不变，但所有时间差区间均跨 0，尾部最大值还可能恶化。"
>
> **若未来研究可观测触发条件**，必须区分两种情形：`n_dirs == 1` 时不存在交会角，只能用补测距离等信息决策；`n_dirs >= 2` 时才能定义"最佳交会角小于 $30^\circ$"。当前不另立该实验。

---

### 2.3 问题三：机会性顺带观测（`OPP_MEASURE`，模块 O）——部分有效，但平均无改善，默认关闭

**动机（正确的机制，来自"已知源位置能否复用"的讨论）**：已定位/已清除的源位置**不改变**其他源的位置先验（源 i.i.d. 均匀，位置之间无关联），但机器人**本来就要去那些清除点停靠**，因此它们是**零额外移动的观测站**。于是机会观测的定位应是：

> 利用既定清除停靠点做观测复用，而不是重构搜索、覆盖或清除调度。

**机会集合**（只取"已发现但尚未认证可清除"的困难频道）：

$$
\mathcal C_{\mathrm{opp}}=\Big\{c:\ \text{state}_c=\text{found},\ \text{未清除},\ \big(\Omega_c>20\text{m}\ \lor\ n_c<2\ \lor\ \alpha_c^{\mathrm{best}}<30^\circ\big)\Big\}
$$

**触发顺序**（先清除、后观测，绝不返回已离开的停靠点；`no_signal` 只记录、不裁剪可行域）：

```text
到达清除点 X → 完成原清除 → 若仍在 X 则执行预选的机会观测 → 更新可行域/状态
```

候选排序用确定性分层键（不引入权重）：`(预计可认证, 交会角接近 90°, Ω 半径大, 与历史观测点基线长, 切换次数少)`；执行顺序按"当前频道优先、下一任务频道最后"减少实际切换。

**配对实验**（$n=400$ 同 seed 同场景；困难子集由**关闭版**的补测阶段 $>600$ s 预先定义，共 175 例，避免选择偏差）：

| 配置 | 全部案例 Δ时间 [95 %CI] | 变快比例 | 困难子集 Δ时间 [95 %CI] | 变快比例 | 机会观测/例 | 认证转化/例 | 额外成本 |
|---|---|---|---:|---|---:|---:|---:|---:|
| O0 关闭（冻结基线） | — | — | — | — | 0 | 0 | 0 |
| O1 每点 ≤1 | **+77.9 [−3, 158]** | 47 % | **−394.5 [−503, −286]** | 73 % | 11.1 | 2.6 | 62 s |
| O2 每点 ≤2 | **+166.2 [89, 243]** | 44 % | −214.5 [−313, −116] | 65 % | 20.0 | 3.8 | 165 s |
| O3 每点 ≤3 | +126.3 [48, 204] | 42 % | −278.1 [−387, −170] | 64 % | 26.0 | 4.3 | 276 s |
| O2u 收紧口径 ≤2 | +27.6 [−52, 107] | 48 % | −381.3 [−491, −272] | 71 % | 5.2 | 0.5 | 42 s |

**全部档全清率 $100\%$、漏清 0 例**（硬约束满足；同坐标同频道不重复测量）。

**结论：默认关闭（`OPP_MEASURE=False`），但机制被证实有效。**

1. **困难子集显著受益**：O1 在困难案例上平均 **−394.5 s（−7.9 %）**，73 % 的案例变快，且补测阶段时间从 832 s 降到 508 s、移动减少 2.2 km。机制清楚——空闲停靠点的额外观测把"未认证"频道直接推入 MEC 认证（$\Omega\le20$m 后清除必然成功），从而省掉一次专门的远距离补测。
2. **全案例平均反而变差**（O1 +78 s、O2 +166 s，且 O2/O3 的 95 %CI 不含 0）：容易案例里 LS 盲清除本来就有约 91 % 成功率、失败归航几乎不花钱，因此额外 $11\sim26$ 次观测（$62\sim276$ s）没有任何可节省的对象；P90 也同步变差（+1 013～+1 175 s）。
3. 收紧口径（只对"当前根本定不了位、必然要走补测"的频道触发）把成本降到 42 s/例，全案例 +27.6 s（CI 跨 0），困难子集 −381 s —— **代价与收益的方向不变，只是规模变小**。
4. 按实验前约定的采纳标准第 6 条（**平均改善低于约 0.5 % 则默认仍关闭**），本模块**不进入冻结版**，仅作为消融结果与"未来可观测触发条件"的记录。
5. 一个必须说明的口径偏差：离线环境的问题三补测负荷（均值 554 s，占 12 %）约为实机（251.7 s，占 6.7 %）的两倍，因此**该模块的收益在离线被高估、成本没有被高估**——真实运行中它只会更不划算。

> **未来若要重启**：正确的方向是把触发条件限制在"**该频道若走补测，其补测点距离必然很远**"这一可观测信号上（即把 §2.2 的补测距离上限与本节合并成"距离大才顺带观测"的条件规则）。这属于新逻辑，需单独立项验证；当前不实施。

---

## 3 评估后保留（冻结）的改进

### 3.1 顺路清除阈值：离线配对 + 实机交错 A/B


**离线配对**（问题三，1000/300 案例扫描）：$\delta$ 从 200 扫到 1200，$\delta=300$ 最优（更大反而回升）；关闭顺路清除最差。这是问题三唯一被证实的有效模块（19 381→18 561 m、4 604→4 389 s）。

**问题四实机交错 A/B**（`300→500→500→300`，并按源数归一，因为各局源数 $10\sim16$ 不等）：

| 按源归一指标 | $\delta=300$（16 局） | $\delta=500$（4 局） | 差 |
|---|---:|---:|---:|
| 虚拟时刻/源 | **788.4 s** | 844.4 s | +56.0 s |
| 顺路时间/源 | 49.7 s | 101.2 s | +51.5 s |
| 扫描后时间/源 | 121.0 s | **90.6 s** | −30.4 s |
| 清除阶段/源（顺路+扫描后） | **170.7 s** | 191.8 s | +21.1 s |
| 顺路成功/源 | 0.4 | 0.6 | +0.3 |
| 归航 episode / 失败清除（每局） | 0.2 / 0.2 | 0.5 / 0.5 | +0.2 / +0.2 |

**结论：保持 $\delta=300$。** $\delta=500$ 的机制确实生效（清除更早前移到扫描内，扫描后时间/源降 $25\%$），但顺路绕行成本（+51.5 s/源、+245 m/源）超过扫描后的节省，**净清除阶段每源反而 +21 s**，且归航/失败清除略有增加。此前"未归一总时间 −528 s"是源数差异造成的假象。

### 3.2 问题四邻域试探圈：四臂配对（每档 400 案例）

| 臂 | 50 % 定向 Δ时间 [95 %CI] | 100 % 定向 Δ时间 [95 %CI] | 归航补回数 | 邻域成功率 |
|---|---|---|---|---|
| 当前 8 m+15 m | — | — | 16 / 33 | 7.5 % / 7.6 % |
| 仅 8 m 圈 | −3 [−38, 33] | +19 [−37, 75] | 12 / 30 | 9.9 % / 10.1 % |
| 仅 15 m 圈 | −1 [−2, −0] | −2 [−3, −1] | 16 / 33 | 14.0 % / 14.7 % |
| 取消邻域（失败即换下一条示向） | +28 [−29, 85] | +21 [−39, 81] | 补回比例降至 96.8 % / 98.1 % | — |

**结论：保持 `(8.0, 15.0)`。** 邻域试探命中率确实很低（$7.5\%\sim7.6\%$），但取消它并不划算：均值略增（CI 含 0），负担转给重复二分归航（episode 178→201、224→236），补回比例下降。"仅 15 m 圈"用一半的清除请求达到相同的补回数、时间微降 1–2 s（收益约 $0.02\%$），**工程收益过小，暂不简化**。圈序交换臂封存，只有实机日志显示邻域阶段请求异常多或耗时明显时才值得继续验证。

### 3.3 三个复杂模块：保持关闭

| 模块 | 离线/实机证据 | 结论 |
|---|---|---|
| 负信息（`no_signal` 收缩联合可行域 + 指向状态） | 离线几乎不触发补测（0.1 次/例），8 例消融与基准完全一致 | 关闭 |
| 改进 PSO 精化定位 | 定位误差 22.85 m → 22.78 m（<0.1 m），而清除半径 20 m | 关闭 |
| 清除后多方向复核 | +11.9 km、+2 536 s，完成率无提升 | 关闭 |
| 概率图排序 / DOP 预筛（问题三） | 概率图排序在 $w=0$ 时 +789 s；DOP 预筛 +84 s | 关闭 |

### 3.4 实现修正：证书正确性优先于小样本 100 %

冻结之前修复了一个**会破坏证书正确性**的高优先级缺陷：问题四的覆盖路径返回坐标序列，程序却把**路径访问序号**写入 `no_signal` 记录，而三角形证书用的是**原始网格编号**（31 个网格点、32 个路径项、原点被重复扫描，访问序号与网格编号仅在 2/32 处偶然相等）。后果是 `no_signal` 被记到错误的三角形顶点上——定向比例 50 %、种子 4026 的第 56 个案例中，真实存在的频道 8 被提前判为"不存在"，最终只清除 13/14。

修复后：路径元素携带真实网格编号（31 点各访问一次、锚点不再重复），并加入常驻回归检查：

- `selfcheck4.py --cert`：路径编号集合 = 全部网格编号、坐标与编号一致、原点出现 1 次；
- 同一批 60 案例：修复前 1 例漏清 / 1 次误排除 → 修复后 **0 漏清 / 0 误排除**；
- 摘要固定输出 `certificate_index_ok`，实机日志据此可判定单局结论是否可用。

配套一致性修正：$\Omega$ 圆域改**确定性外接多边形**（$R_c=R/\cos(\pi/m)$，与问题一、问题三口径统一）；任务级 2-opt 补齐开放路径尾段反转；清除任务一律携带频道编号（消除同坐标互相覆盖）；归航改为返回布尔值并区分"当前位置未试过/已试过失败"。

**方法论要点**：小样本 100 % 的清除率**不能替代**证书逻辑检查——上述缺陷在 30 例离线复核中并未暴露。

---

## 4 稳定性与验收口径

### 4.1 实机日志稳定性

| 问题 | 有效新日志 | 平均时间 | 标准差 | 变异系数 | 范围 |
|---|---:|---:|---:|---:|---|
| 问题三 | 7 局 | 3 997.5 s | 522.2 s | 13.1 % | 3 363.0–4 708.8 s |
| 问题四 | 11 局 | 10 784.0 s | 701.8 s | 6.5 % | 10 057.9–12 676.6 s |
| 问题四（剔除单个长尾局） | 10 局 | 10 594.7 s | 330.7 s | 3.1 % | 10 057.9–11 168.5 s |

全部有效日志满足：HTTP 请求 `accepted=true`、`request_id` 唯一、`unresolved_channels=0`、问题四 `certificate_index_ok=true`、清除失败均被后续归航补回。**因此通信链路与状态机稳定，时间波动来自随机场景规模与定向难度，不是程序随机失稳。**

### 4.2 归一化指标（避免"用随机场景总时间比稳定性"）

问题三各局源数 $10\sim16$；以源数回归得

$$
T_3 \approx 1370 + 211.4\,N_s ,
$$

源数单独解释约 $92.1\%$ 的整局时间方差（残差标准差 ≈ 161 s），且总时间与移动距离相关系数达 $0.999$、与检测次数相关性很弱。故问题三应采用**扫描后归一化时间**

$$
T_{\text{post/source}}=\frac{T_{\text{total}}-T_{\text{coverage}}}{N_s},
$$

实测约 $164\sim204$ s/源，比原始总时间稳定得多。问题四的源数量不足以解释时间（定向性与可见方向更重要），应按**是否触发补测/归航**分层报告，并关注 P90/P95/最大值。

### 4.3 正式测试前的验收标准（8 条）

1. 全清率 $100\%$；
2. `unresolved_list = []`；
3. HTTP 拒绝数为 0；
4. 问题四 `certificate_index_ok = true`；
5. 固定种子重复运行结果完全一致（已落地为 `selfcheck4.py --cert` 的常驻检查：20 例逐案例逐指标一致）；
6. 分场景统计关注 P90、P95 与最大值，而非仅均值；
7. 问题三按源数量分层（$T_{\text{post/source}}$）；
8. 问题四单独报告是否触发补测/归航。

---

## 5 复现命令

```powershell
# 问题三：离线自检 + 定位/清除来源诊断
python problem3_robot\selfcheck.py --cases 1000 --diag
# 问题三：模块O 机会性顺带观测五臂配对(O0/O1/O2/O3/收紧口径)
python problem3_robot\ablation3.py --opp 400
# 问题三：LS 试探清除门限配对实验
python problem3_robot\ablation3.py --ls-gate 300
# 问题三：外部模块消融（概率图排序 / DOP 预筛 / 顺路清除）
python problem3_robot\ablation3.py 300

# 问题四：证书与索引回归（含固定种子一致性、摘要 schema）
python problem4_robot\selfcheck4.py --cert 60
# 问题四：分阶段归因
python problem4_robot\selfcheck4.py 30 --diag
# 问题四：邻域试探四臂消融 / 补测距离上限四臂消融 / 顺路阈值配对
python problem4_robot\ablation4.py --neighbor 400
python problem4_robot\ablation4.py --supp-cap 400
python problem4_robot\ablation4.py --onway-paired 100

# 实机日志对照（δ A/B 风险复核；含按源归一与长尾计数）
python problem4_robot\compare_runs.py "problem4_robot\logs\p4_log_*.jsonl"
python problem4_robot\compare_runs.py --pair 300 500 "problem4_robot\logs\p4_log_*_d*.jsonl"
```

实机运行（默认值即冻结值，无需任何附加参数）：

```powershell
python problem3_robot\robot.py     --robot-id <队号>
python problem4_robot\robot4.py    --robot-id <队号>
```

---

## 6 本章小结

1. 问题三、问题四的算法结构**正式冻结**；所有阈值一律回到保守值（$\delta=300$、`SUPP_MAX_DIST=None`、`NEIGHBOR_RINGS=(8,15)`、三个复杂模块关闭）。
2. 试过并**放弃**三项改进：问题三的 LS 信任门限（单调劣化，最紧档 +2 646 s / +12 km）、问题四的补测距离上限（全清率不变但时间差区间全跨 0，尾部最大值可能恶化）、问题三的机会性顺带观测（困难子集显著受益 −394 s，但全案例平均 +78～+166 s，按约定标准默认关闭）。共同教训是：**可观测的量（$\Omega$ 半径、补测距离、空闲停靠点）不等于决策所需的信息（该次补测/清除能否成功），而"平均更差、局部更好"的模块必须用分层口径评价。**
3. 稳定性必须用**归一化口径**（问题三 $T_{\text{post/source}}$、问题四分层 P90/P95/最大值）与**硬约束**（全清率、`unresolved`、证书索引、固定种子一致性）评价，而不是比较随机场景的总时间。
4. 小样本 $100\%$ 不能替代正确性检验：证书索引缺陷只在扩大样本 + 逐动作核对后才暴露。
