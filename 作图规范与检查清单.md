# 科研作图规范与检查清单（长期记忆 · 每次出图前必读）

> 本文件由用户明确提出并要求"时刻牢记"。凡是生成论文/报告配图，必须逐条对照执行。

## 0. 文件存放（硬性规则）
- 所有由 Origin（或其他绘图软件）生成的图片文件（`.png/.svg/.pdf/.tif/.emf`）与工程文件（`.opju`），
  一律放入项目根目录下的专用文件夹：`D:\My_MathModeling_Project\origin_figures\`
- **禁止**把图存进 `corpus\`（语料/文本分析目录）内部。corpus 只放文字分析与表格类资料。

## 1. 配色（硬性规则：颜色必须"所见即所得"）
- 参照某张图片/模板配色时，**必须以图片真实像素为准**：用取色工具（如 vision_dominant_colors）
  对目标色块逐块采样，得到该图真实 RGB；不能直接相信"读图模型"口头转述的色值
  （模型可能背书常见模板而并非读你这张图的实际颜色）。
- 多分类/多系列尽量用**连续渐变或色相渐进**的配色，避免"红橙黄绿蓝靛紫"式色相跳变的彩虹条。
- 用色要服务于信息传达：数值高低/重要性可通过同一色系的深浅、明度渐变来直观表达。

## 2. 避免"图表裸奔"（图表必须有编号、标题与说明）
- 每张图必须携带：
  1. **图编号**：如"图 1"（报告中与正文引用一致）；
  2. **完整、自解释的标题**（读者不看正文也能知道图在讲什么）；
  3. 必要时的**图例**（多系列必须可区分且图例含义明确）；
  4. 数据来源/实验条件等说明（按需，可在脚注或正文）。
- 严禁只有坐标轴+曲线、无任何文字说明就放进文档。

## 3. 要素完整（坐标轴与量纲）
- X/Y 轴必须标注**变量名 + 单位**（如"论文数量（篇）"、"时间（min）"、"Kd（mL/g）"）。
- 刻度范围合理、步长规整；类别轴标签与数据一一对应，无歧义。
- 若同一图有双 Y 轴/多量纲，必须各自标明单位并区分样式。

## 4. 风格与分辨率
- 禁止默认丑陋配色；调色板统一、协调、符合投稿或汇报要求。
- 出图分辨率 ≥ 300 dpi；禁止拉伸变形、模糊、被压缩到失真。
- 字号清晰可读，线与符号粗细适中；期刊图注意单栏/双栏宽度（如单栏 89 mm）。
- 网格、边框、刻度方向（向内/向外）按期刊或模板统一，不用默认值蒙混。

## 5. 类型多样与图文呼应
- 同一报告/论文中图表类型要**多样**（柱/折/散/箱线/雷达/热图…组合使用），不要全篇只有一种。
- 每张图都应在正文中被引用并解读（"图 X 显示……说明……"），杜绝"图文脱节"。
- 图表数据必须与正文数值一致、可追溯。

## 6. 出图流程自检（每次完成后逐项打勾）
- [ ] 文件存放在 `origin_figures\`
- [ ] 配色经过像素级提取校验（如需仿模板）
- [ ] 有编号 + 标题（+ 必要的图例/来源）
- [ ] 坐标轴有变量名与单位、刻度清晰
- [ ] 300 dpi、无变形、无模糊、风格统一
- [ ] 图表类型有变化、正文有解读
- [ ] 视觉复核：导出后用视觉工具把图"读回来"核对（颜色/标签/越界/图例）
