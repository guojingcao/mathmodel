# Paper figure template

Use `cumcm_panel()` to create the canvas, draw into its axes with `ax=`, export through `save_cumcm()`, and put the formal figure caption in Word/LaTeX rather than inside the plotted axes.
